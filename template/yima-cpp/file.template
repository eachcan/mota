#pragma once

// 此文件由 Mota 代码生成器自动生成
// 生成时间: <%=CURRENT_TIME%>
// 源文件: <%=SOURCE_FILE%>

#include <QCborMap>
#include <QCborArray>
#include <QCborValue>
#include <QString>
#include <QStringList>
#include <QVariant>
#include <QVector>
#include <QSharedPointer>
#include <memory>

#include "framework/IBlock.h"
#include "framework/IModel.h"
#include "framework/IAnnotation.h"

<%if (INCLUDES.count > 0)%>
<%foreach INCLUDES.files as file%>
#include "<%=file.translated%>"
<%endforeach%>
<%endif%>

using namespace ymf;

<%if (NAMESPACE.exists)%>
<%call namespace_declaration%>
<%endif%>

<%call test%>

<%foreach DECLARATIONS as declaration%>
// DEBUG: Processing type: <%=declaration.type%>, name: <%=declaration.name%>
<%if (declaration.type == "enum")%>
// Found enum, calling enum misc
<%call enum%>
<%endif%>

<%if (declaration.type == "struct")%>
// Found struct, calling struct misc
<%call struct%>
<%endif%>

<%if (declaration.type == "block")%>
// Found block, calling block misc  
<%call block%>
<%endif%>

<%if (declaration.type == "annotation_decl")%>
// Found annotation_decl, calling annotation_decl misc
<%call annotation_decl%>
<%endif%>

<%endforeach%>

<%if (NAMESPACE.exists)%>
<%call namespace_closing%>
<%endif%> 